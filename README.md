# image-translator-extension
Design and implement a Chrome Extension (Manifest V3) that lets users draw a rectangular region on any webpage, performs screen capture → OCR → translation, and renders results in a movable semi-transparent overlay on the same page.


Steps :
1. init project - with  `npx wxt@latest init`  
2. install node modules
3.